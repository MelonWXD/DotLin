<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="h_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:background">@color/gray</item>
    </style>

    <style name="padding_divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
        <item name="android:paddingLeft">@dimen/lay_32</item>
        <item name="android:background">@color/gray</item>
    </style>


    <style name="toolbar_image">
        <item name="android:padding">@dimen/lay_4</item>
        <item name="android:layout_margin">@dimen/lay_4</item>
        <item name="android:layout_width">@dimen/lay_36</item>
        <item name="android:layout_height">@dimen/lay_36</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:visibility">invisible</item>
    </style>

</resources>
